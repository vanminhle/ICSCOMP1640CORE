@model ICSCOMP1640CORE.Models.Idea
@{
    ViewBag.Title = "Idea Details";
}

@*
<div class="card">
    <h2 style="text-align: center" class="card-header">Idea Details</h2>
    <div class="card-body">
        <p>Idea Name: @Model.IdeaName</p>
        <p>Content Of Idea: @Model.Content</p>
        <p>Category: @Model.Category.Name</p>
        <p>Submit Date: @Model.SubmitDate.ToString("yyyy-MM-dd")</p>
    </div>
</div>
<center><a class="btn btn-primary col-2 fas fa-reply mt-4" asp-controller="Coordinators" asp-action="GetIdeaFromCoor"></a></center>*@

<div style="text-align:center;">
    <h1>@Model.IdeaName</h1>
    <div>
        <p>Author: @Model.User.UserName</p>
        <p>Submit Date: @Model.SubmitDate.ToString("yyyy-MM-dd hh:mm tt")</p>
    </div>
    <div class="row" style="margin-left:auto; margin-right:auto">
        <a class="btn btn-info btn-outline-warning col-4">DOCUMENT - CLICK TO DOWNLOAD</a>
        <p class="col-6"></p>

    </div>
    <div class="card text-white bg-secondary mb-3">
        <div class="card-body">
            <h5 class="card-title">@Model.Content </h5>
            <p class="card-text"></p>
        </div>
    </div>
    <div class="card text-white bg-secondary mb-3">
        <div class="card-body">
            <p class="card-text">Comment</p>
        </div>
    </div>
</div>

<a class="btn btn-info btn-outline-warning col-2 fas fa-reply mt-4" asp-controller="Coordinators" asp-action="GetIdeaFromCoor"></a>