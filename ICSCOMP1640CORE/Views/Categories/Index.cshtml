@model IEnumerable<ICSCOMP1640CORE.Models.Category>

<h1 style="text-align: center; margin-bottom: 30px; border-bottom: 1px solid #ccc; line-height: 5rem">Category</h1>
<main>
    <div>
        @using (Html.BeginForm("Index", "Categories", FormMethod.Get))
        {
            <div style="text-align: right">

                    @Html.TextBox("searchCategory", null, new {@placeholder = "Find By Name" })
                    <input type="submit" value="Search" style="margin-left: -5px" />
            </div>
        }
    </div>
    <br>
    <br>
    <table style="text-align: center" class="table">
        <thead class="thead-dark">
            <tr>

                <th scope="col">Category</th>
                <th scope="col">Description</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>

                    <td>@item.CategoryName</td>
                    <td>@item.Description</td>
                    <td>
                        @Html.ActionLink("", "DeleteCategory", "Categories", new { Id = item.CategoryId }, new { @class = "btn btn-outline-danger mb-3 col-sm-3 fas fa-trash-alt space" })
                        @Html.ActionLink("", "EditCategory", "Categories", new { id = item.CategoryId }, new { @class = "btn btn-outline-info mb-3 col-sm-3  	fas fa-edit" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <center><a class="btn btn-outline-success fas fa-plus " asp-area="" asp-controller="Categories" asp-action="CreateCategory"> Add new one category</a></center>
</main>
